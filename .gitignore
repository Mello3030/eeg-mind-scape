# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Env
.env
!.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# ML/ is a separate git repository (github.com/Sanchit-Raut/Major) checked out
# inside this one. Tracking it from here would commit a gitlink pointing at a
# submodule that was never registered, which breaks every fresh clone. Ignore it
# until it is either registered as a real submodule or absorbed.
ML/

# Superseded by ML/backend - kept on disk for reference, not tracked.
server/
ml-service/
